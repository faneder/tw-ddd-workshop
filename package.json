{
  "name": "cart-problems",
  "version": "1.0.0",
  "description": "DDD Workshop repo",
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "scripts": {
    "test": "jest --config ./test/jest.config.js --silent -w=1 test",
    "build": "npm run clean && tsc && copyfiles -u 1 src/**/*.json dist",
    "clean": "rimraf dist",
    "console": "ts-node --project tsconfig-console.json"
  },
  "author": "No one",
  "dependencies": {
    "copyfiles": "^2.4.1",
    "ts-money": "^0.4.7"
  },
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "jest": "~27.0.5",
    "ts-jest": "^27.0.0",
    "ts-node": "^8.10.1",
    "typescript": "^4.1.3"
  }
}
